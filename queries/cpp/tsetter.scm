;; ===================
;; Action Queries
;; ===================
; inherits: c
;; C++ shares the bulk of its syntax with C, so we re-use the C query file
;; verbatim via tree-sitter's `; inherits:` mechanism (see
;; https://tree-sitter.github.io/tree-sitter/using-parsers#language-inheritance).
;; Any C++-only idiosyncrasies (templates, namespaces, ...) can be added below.
